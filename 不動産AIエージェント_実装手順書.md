# 不動産AIエージェント（Next + Vercel AI SDK）実装手順書

ポートフォリオ作品⑥ / AIエージェント枠（任意・余力があれば） / 最終更新: 2026/07/16

---

## 確定事項（2026/07/16 実装前レビューで決定）

- **構成**: ⑥は新規Next.jsアプリとして実装する。④のAPIはコードを同居させず、**デプロイ済みの外部サービスとしてHTTPで叩く**（④の実体は別リポジトリ `realestate-api`）
- **順序**: 先に④をVercel + Neonにデプロイ（シードデータ投入まで）し、⑥は④の本番URLを叩く
- **モデル**: `gemini-3.5-flash`（Google AI Studio 無料枠）。レート制限が問題になったらFlash-Lite系にフォールバック
  - 無料枠の注意: 入力データはGoogleの製品改善に使われる（扱うのはデモ用シードデータのみなので許容）。レート制限の実値はAI Studioの管理画面で確認する
- **品質の柱を2つ追加**: ①エージェント評価テスト（evals）、②本格的なhuman-in-the-loop（承認引数と実行引数の一致検証）。詳細はセクション4とフェーズ6.5
- **観測可能性**: Langfuse（Cloud Hobbyプラン=無料・月5万ユニット・保持30日）でトレーシングを導入。Vercel AI SDKとOpenTelemetryベースで公式統合

---

## 0. この作品の位置づけ（ブレないための前提）

- **役割**: 「AIが判断してツールを呼び、多段で仕事を進める（agentic）」を証明する枠。②（検索）との対比で「検索 vs 行動」の2軸を作る
- **着手タイミング**: **④の後（早くて8月以降）**。優先度は低め＝「やれたら強い追加点」。①②③の完成と④⑤を圧迫してまで前倒ししない
- **最大の強み**: **④で自作した本物のAPIを、自作エージェントがツールとして叩く**。「APIを設計できる」と「その上にエージェントを載せられる」を同時に、嘘なく証明する。④と⑥が互いの価値を引き上げ合う
- **物語**: 前職の不動産営業ドメインが、エージェントの「判断ロジック」に乗る

### 評価される核（ここがブレると"凝ったチャットボット"になる）
評価されるのは:
- AIが1往復で終わらず、**複数ツールを連鎖**させて仕事を進める
- 状況に応じて**次の手を変える判断**（例: 検索ゼロ件なら条件を緩めて再検索）
- 書き込み系を**人間の承認を挟んで**安全に実行する（human-in-the-loop）

### やらないこと / スコープの釘
- チャットで質問に答えるだけにしない（②と被る・エージェントにならない）
- 実データの地理空間API（国交省ライブラリ等）は**今回は使わない**（GeoJSON/タイル等の学習コストが主役を食う・⑥を複雑化させすぎる）
- ツールの種類を闇雲に増やさない。多段と判断が見える最小セットに絞る

---

## 1. 技術スタック

| レイヤー | 採用 | 備考 |
|---|---|---|
| フロント/FW | Next.js (App Router) | |
| エージェント | Vercel AI SDK の tool calling | `tool()` でツール定義、`streamText` のマルチステップ実行 |
| モデル | **`gemini-3.5-flash`（無料枠）** | Google公式でagentic用途の推奨モデル。tool callingの安定性を優先 |
| ツールの実体 | **④のHono API**（不動産業務管理・デプロイ済み） | 別リポジトリ `realestate-api`。本番URLをHTTPで叩く |
| UI | React + Tailwind | ツール実行の途中経過を可視化 |
| 観測 | **Langfuse Cloud（無料枠）** | OpenTelemetry経由でAI SDKのモデル呼び出し・ツール実行・多段フローを自動トレース |
| デプロイ | Vercel | |

---

## 2. エージェントに持たせるツール群（④のAPIを道具化）

④の不動産業務管理APIのエンドポイントを、AIが呼べるtoolとして定義する。**読み取り系と書き込み系を分ける**（human-in-the-loop設計に効く）。

### ツール⇔④API対応表（認証要否つき・2026/07/16調査済み）

| ツール | 種別 | ④のエンドポイント | 認証 |
|---|---|---|---|
| `searchProperties` | 読み取り | `GET /properties` | 不要（公開） |
| `getPropertyDetail` | 読み取り | `GET /properties/:id` | 不要（公開） |
| `checkViewingAvailability` | 読み取り | **④に専用エンドポイントを新規追加する** | 公開で設計 |
| `createInquiry` | 書き込み（要承認） | `POST /properties/:id/inquiries` | 不要（公開・レート制限あり） |
| `createViewing` | 書き込み（要承認） | `POST /inquiries/:id/viewings` | **要JWT（エージェント権限）** |

- 読み取り系はAIが自由に呼んでよい。書き込み系は実行前に必ずユーザー承認を挟む
- **`createViewing`の認証設計**: デモ用エージェントアカウントのJWTを**⑥のサーバー側（Route Handler内）でのみ**取得・保持して④に渡す。トークンをクライアントに一切出さない。ここは加点にも減点にもなり得る箇所（雑にやると即減点）
- **④への空き枠エンドポイント追加**は `realestate-api` 側の作業。「エージェントの要求で既存APIを進化させた」という物語としてREADMEに書く

> 各ツールは zod でパラメータスキーマを定義し、AI SDKの`tool()`に渡す。引数の型と説明を丁寧に書くほど、AIが正しく呼べる。

---

## 3. 見せる多段フロー（エージェントの核）

理想的なシナリオ例:

> ユーザー「予算8万以内、駅近2LDK、ペット可で探して。良さそうなのがあれば内見も入れたい」
>
> 1. 条件を解釈（予算/間取り/エリア/ペット可）
> 2. `searchProperties` を呼ぶ
> 3. **結果が多ければ条件を絞る / ゼロなら条件を緩めて再検索**（← 判断の見せ場）
> 4. 候補を提示し、気になる物件の `getPropertyDetail` を呼ぶ
> 5. `checkViewingAvailability` で空き枠確認
> 6. 内見予約は**実行前にユーザーに確認** → OKなら `createViewing`

「ゼロ件なら条件を緩めて再検索」のような**状況に応じて次の手を変える**部分を最低1つ必ず入れる。これが単なるツール呼び出しとエージェントの分かれ目。

---

## 4. 品質で差をつける5つの設計判断

ここが「凝ったチャットボット」と「製品レベルのエージェント」を分ける:

1. **途中経過のストリーミング可視化**
   - AIが今どのツールを、どんな引数で呼んでいるかをUIに逐次表示（例:「物件を検索中… 条件: 予算8万/2LDK/ペット可」）
   - 思考過程・ツール実行が見えると一気に製品っぽくなる
2. **human-in-the-loop（書き込み前の承認・引数一致検証まで）**
   - 内見予約・問い合わせ作成は、AIが勝手に実行せず**ユーザーの承認ボタンを挟む**
   - 承認UIを置くだけで終わらせず、**「ユーザーが承認した引数」と「実際に④へ送られる引数」の一致をサーバー側で検証する**（承認後の改ざん・すり替えを防ぐ）
   - 実務でエージェントを安全に運用する必須パターン＝「分かってる人」のシグナル
3. **失敗時のハンドリング**
   - ツールがエラー（検索ゼロ件 / API落ち / タイムアウト / レート制限429）を返したとき、AIがリカバリするか、正直に「できなかった」と返す
   - エラーを握りつぶさない
4. **エージェント評価テスト（evals）** ← 2026/07/16追加・最大の差別化点
   - プロンプトを変えると挙動が変わる非決定的なエージェントを、**Vitestで回帰テスト**する
   - ツール呼び出し列をアサートする（例:「ゼロ件シナリオで条件を緩めた再検索が発生する」「承認なしに書き込みツールが実行されない」）
   - 「AIの非決定性とどう付き合うか」に答えを持っているポートフォリオは少数派。④のVitest実績と物語がつながる
5. **観測可能性（Langfuseトレーシング）** ← 2026/07/16追加
   - 「作れる」だけでなく**「運用・観測できる」**を示すLLMOpsのシグナル。モデル呼び出し・ツール実行・多段フローがネストされたスパンで可視化される
   - プロンプト調整やevalsのデバッグにも実利（どのステップで何がどの引数で呼ばれたか追える）。トレース画面はREADMEのスクリーンショット素材にもなる
   - あくまで脇役。ダッシュボードの作り込みには時間をかけず「導入→トレースが見える→READMEで語る」で止める

---

## 5. 実装手順

### フェーズ-1: ④のデプロイ（`realestate-api`側の作業・⑥着手の前提）
1. ④をVercel + Neonにデプロイし、シードデータを投入
2. デモ用エージェントアカウントを用意（⑥の`createViewing`が使う）
3. 空き枠確認の専用エンドポイントを④に追加（`checkViewingAvailability`の実体）

### フェーズ0: 環境構築
1. Next.js (App Router) プロジェクト、Vercel AI SDK 導入
2. モデル（`gemini-3.5-flash`）のAPIキー設定（環境変数。**secretsコミットしない**）
3. ④の本番URL・公開/認証エンドポイントを整理（ベースURLは環境変数化）

### フェーズ1: 単発のツール呼び出し
1. `searchProperties` 1つだけ定義（zodスキーマ＋④のGETを叩く）
2. 「物件を検索して」で1ツールが呼ばれ、結果が返ることを確認
> まず1ツールが正しく動く土台を作る（多段はその後）

### フェーズ2: 読み取り系ツールを揃える
1. `getPropertyDetail` / `checkViewingAvailability` を追加
2. `streamText` のマルチステップで、検索→詳細→空き確認が連鎖することを確認

### フェーズ3: 判断ロジック
1. ゼロ件時に条件を緩めて再検索する挙動をプロンプト＋ツール設計で実現
2. 候補が多いときの絞り込み

### フェーズ4: 書き込み系 + human-in-the-loop
1. `createInquiry` / `createViewing` を定義
2. 書き込み実行前に「この内容で予約しますか？」の承認UIを挟む
3. 承認後に④のPOSTを叩き、結果を反映
4. **承認した引数と実行時の引数の一致をサーバー側で検証する**（承認済みペイロードの照合）
5. `createViewing` 用のJWTは⑥のサーバー側でのみ取得・保持（クライアントに出さない）

### フェーズ5: UI（途中経過の可視化）+ 観測
1. ツール呼び出し・引数・結果をストリーミングで逐次表示
2. 会話履歴・候補物件カード・承認ダイアログ
3. Langfuse導入（Cloud無料枠）: `instrumentation.ts` にOpenTelemetry SDK＋`LangfuseSpanProcessor` を登録
   - AI SDKのバージョンに注意: v6は呼び出しごとに `experimental_telemetry: { isEnabled: true }`、AI SDK 7はデフォルト発行＋`@langfuse/vercel-ai-sdk`
   - **Vercelのサーバーレスでは関数終了前に `forceFlush()`** でトレース送信を確実にする（この対応自体をREADMEで語る）
   - Langfuseのキーは環境変数管理（secretsコミットしない）

### フェーズ6: 失敗ハンドリング
1. ツールエラー時のAIのリカバリ／正直な報告
2. ④のAPIが落ちている場合のフォールバック表示
3. **モデルフォールバック**: Gemini APIの429/503時に `gemini-3.5-flash` → `gemini-3.1-flash-lite` へ自動切替（2026/07/17決定）
   - 無料枠のレート制限はモデルごとに別枠のため、実効枠が合算される
   - あわせて`maxRetries`を下げ、リトライによる無料枠（RPM 5 / RPD 20）の浪費を抑える
4. **無料枠枯渇時の正直な表示**: 全モデルの枠を使い切ったら「本日のデモ枠を使い切りました」と表示（ポートフォリオなので自前のセッション制限までは作り込まない）

### フェーズ6.5: エージェント評価テスト（evals）
1. Vitestでエージェントの回帰テストを実装。ツール呼び出し列をアサートする:
   - 「検索ゼロ件のシナリオで、条件を緩めた再検索（2回目の`searchProperties`）が発生する」
   - 「承認イベントなしに`createViewing`/`createInquiry`が実行されない」
   - 「承認した引数と実行された引数が一致する」
2. ④のAPIはモックに差し替えてシナリオを固定する（実APIに依存しない決定的なテストにする）
3. READMEで「非決定的なエージェントをどう回帰テストするか」を語る

### フェーズ7: 仕上げ（README→デプロイ→GIFの順）
1. README（**②=検索 / ⑥=行動 の対比、④APIをツール化した設計、human-in-the-loopの意図**）
2. デモ用のシードデータ（④側に物件が入っている前提を整える）
3. デプロイ

### フェーズ8: サイト化（2026/07/18採用決定・デプロイ後に実施）
「不動産情報サイトの中にエージェントが同居する」形へ拡張する。**リポジトリは統合しない**（④=独立API、⑥=サイト+エージェントのフロント、の構図を維持）。

1. ④側: 物件画像対応（スキーマに画像フィールド追加・マイグレーション・シード更新。物件は50件に拡充、画像はユーザーがAI生成）
2. ⑥側: 物件一覧ページ（カード＋画像＋簡易フィルタ）と物件詳細ページを追加（④の公開APIから取得）
3. チャット内の物件提示から詳細ページへの導線、サイトからチャットへの導線
4. スコープの釘: 会員機能・お気に入り・地図は**作らない**。一覧/詳細/フィルタまで
5. README更新: 「検索UI（自分で探す） vs エージェント（AIに任せる）を1製品内で対比」の物語を追記

---

## 6. README に盛り込む論点（実質の履歴書）

- ②（RAG検索）との違い: ②は「検索して返す」、⑥は「AIが判断してツールを呼び行動する」
- **④で自作したAPIをツールとして使った**点（API設計力とエージェント設計が一本に乗る）
- **エージェントの自動評価（evals）**: 非決定的なAIの挙動を、ツール呼び出し列のアサートで回帰テストした設計
- 読み取り系/書き込み系を分け、書き込みはhuman-in-the-loopで安全にした設計判断（承認引数と実行引数の一致検証まで踏み込んだ点）
- **エージェントの要求で④のAPIを進化させた**点（空き枠確認エンドポイントの追加）
- `createViewing`の認証設計（JWTをサーバー側でのみ扱い、クライアントに出さない）
- 状況に応じて次の手を変える多段の実装（ゼロ件時の再検索など）
- **Langfuseによる観測可能性**: 多段ツール連鎖のトレース（スクリーンショット掲載）と、serverlessでのflush対応
- 前職の不動産営業ドメイン知識を判断ロジックに反映した点
- 無料枠モデル（`gemini-3.5-flash`）の制約（レート制限・データ利用）と、その割り切りの説明

---

## 7. チェックリスト

- [ ] ④のデプロイ後に着手（⑥は④の本番URLを叩く）
- [ ] ④に空き枠確認エンドポイントを追加（`realestate-api`側）
- [ ] 多段＋「状況で次の手を変える判断」を最低1つ入れる
- [ ] 書き込み系は必ずhuman-in-the-loop（承認を挟む＋承認引数と実行引数の一致検証）
- [ ] evalsで「条件緩和の再検索」「承認ゲート」を回帰テスト
- [ ] `createViewing`のJWTをクライアントに出さない
- [ ] ツール実行の途中経過をUIで可視化
- [ ] Langfuseトレーシング導入（serverlessの`forceFlush()`対応・キーは環境変数）
- [ ] ツールエラーを握りつぶさない
- [ ] secrets（モデルキー・④の認証情報）をコミットしない
- [ ] ②との違い（検索 vs 行動）をREADMEで明確に
- [ ] 優先度は低め。①②③④⑤を圧迫しない範囲で
